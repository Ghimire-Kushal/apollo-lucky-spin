# Required for online persistence (Neon Postgres connection string)
DATABASE_URL=postgres://user:pass@host/dbname?sslmode=require
# Optional: keeps admin logins valid across restarts
SESSION_SECRET=change-me-to-a-long-random-string
# Optional: initial admin credentials (only used on first seed)
ADMIN_USER=admin
ADMIN_PASS=apollo
